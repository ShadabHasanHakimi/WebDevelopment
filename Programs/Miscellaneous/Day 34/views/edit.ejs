<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
</head>
<body>
    <h2>Edit your post</h2>
    <p>Post id: <%= post.id %></p>
    <p>Post username: <%= post.username %></p>
    <!-- HTML forms directly only supports get and post request only and do not supports requests such as patch, delete, put etc. -->
    <!-- To do so, we have to install a npm package "method-override" -->
    <form method="post" action="/posts/<%=post.id%>?_method=PATCH">
        <!-- name of the textarea should be 'content' -->
        <textarea name="content" id=""><%= post.content %></textarea>
        <button type="submit">Submit</button>
    </form>
</body>
</html>