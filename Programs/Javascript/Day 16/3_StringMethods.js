// str.trim() : Removes whitespaces from both the ends of string & returns a new one
// does not remove whitespaces from between the string
// It does not changes the original string, instead it creates a new string without spaces
let str1 = "   Hello  Everyone     ";
console.log(str1);

console.log(str1.trim());

// Note: String are Immutable in JS
